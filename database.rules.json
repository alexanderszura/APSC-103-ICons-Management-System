{
  "rules": {
    ".read": "auth != null",

    "staff": {
      "$uid": {
        ".write": "auth != null && (
          root.child('staff').child(auth.uid).exists() || 
          (
            auth.uid === $uid &&
            auth.token.email != null &&
            root.child('allowed_emails')
              .child(auth.token.email.replace('.', ',').replace('@', '|'))
              .exists()
          )
        )"
      }
    },

    "allowed_emails": {
      ".read": "auth != null",
      "$emailKey": {
        ".write": "auth != null && (
          root.child('staff').child(auth.uid).exists() ||
          (
            auth.token.email != null &&
            $emailKey === auth.token.email.replace('.', ',').replace('@', '|')
          )
        )"
      }
    },

    "development": {
      ".read": "auth != null",
      ".write": "auth != null && root.child('staff').child(auth.uid).exists()"
    },

    "production": {
      ".read": "auth != null",
      ".write": "auth != null && root.child('staff').child(auth.uid).exists()"
    }
  }
}