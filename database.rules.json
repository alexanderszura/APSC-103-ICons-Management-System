{
  "rules": {
    ".read" : "auth != null && (root.child('staff').child(auth.uid).exists() || root.child('admins').child(auth.uid).exists())",
    ".write": "auth != null && (root.child('staff').child(auth.uid).exists() || root.child('admins').child(auth.uid).exists())"
  }
}